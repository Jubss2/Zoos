using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Pared : MonoBehaviour
{
    // Start is called before the first frame update
    public enum ParedeType
    {
        L, R, Up, Down
    }

    public ParedeType Patype;
   
}
